# Supabase 配置
# 用於認證模式和 AI 功能
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# API 基礎 URL（可選，預設為 http://localhost:3001）
VITE_API_BASE=http://localhost:3001

# 注意：安全測試頁面相關的環境變數不應出現在此文件中
# 安全測試頁面僅在開發環境自動啟用，或需要特殊測試場景時才設置
# 生產環境必須顯式允許，防止誤上線
